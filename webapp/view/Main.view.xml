<mvc:View
    controllerName="customlib.controller.Main"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    core:require="{
        formatter: 'customlib/model/formatter'
    }">

    <Page
        title="{i18n>appTitle}"
        id="page">

        <content>
            <IllustratedMessage
                title="{i18n>appTitle}"
                illustrationType="sapIllus-SuccessHighFive"
                enableVerticalResponsiveness="true"
                description="{i18n>appDescription}">

                <additionalContent>

                    <Button
                        id="helloButton"
                        text="{formatter: 'formatter.formatValue', path: 'i18n>btnText'}"
                        press="sayHello"
                        class="sapUiMediumMarginEnd" />

                    <Button
                        id="mathButton"
                        text="Test BasicMath Library"
                        press="onTestMath"
                        type="Emphasized" />

                </additionalContent>

            </IllustratedMessage>
        </content>

    </Page>

</mvc:View>
