_schema-version: "3.2"
ID: customlib
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: customlib-destination-content
  type: com.sap.application.content
  requires:
  - name: customlib-destination-service
    parameters:
      content-target: true
  - name: customlib-repo-host
    parameters:
      service-key:
        name: customlib-repo-host-key
  - name: customlib-uaa
    parameters:
      service-key:
        name: customlib-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: customlib_html_repo_host
          ServiceInstanceName: customlib-html5-service
          ServiceKeyName: customlib-repo-host-key
          sap.cloud.service: customlib
        - Authentication: OAuth2UserTokenExchange
          Name: customlib_uaa
          ServiceInstanceName: customlib-xsuaa-service
          ServiceKeyName: customlib-uaa-key
          sap.cloud.service: customlib
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: customlib-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: customlib-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - customlib.zip
      name: customlib
      target-path: resources/
- name: customlib
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: customlib-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: customlib-destination-service
    service-plan: lite
- name: customlib-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: customlib-xsuaa-service
    service-plan: application
- name: customlib-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: customlib-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
